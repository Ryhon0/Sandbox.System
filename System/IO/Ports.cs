namespace Sandbox.System.IO
{
    public enum SearchOption
    {
        TopDirectoryOnly,
        AllDirectories
    }

    public enum FileAccess
    {
        Read,
        Write,
        ReadWrite
    }
}